import { LanguagesFilterPipe } from './languages-filter.pipe';

describe('LanguagesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguagesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
