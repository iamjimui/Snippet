import { SnippetFilerPipe } from './snippet-filer.pipe';

describe('SnippetFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new SnippetFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
